/*** Take in a string (plain text or HTML marked) and estimate read time ***/
function estReadTime(string){
	var readSpeed = 250;      // Estimated speed of reader in Words Per Minute (wpm)
	var imageTime = 10;        // Estimate of time taken up by pictures (seconds)

	var readSpeed = 250/60; // Convert to words per second
}
